# 像素幸存者 (Pixel Survivor) — 简历项目描述与面试准备指南

---

## 目录

- [一、简历项目描述（按岗位定制）](#一简历项目描述按岗位定制)
  - [后端开发岗](#后端开发岗位)
  - [全栈开发岗](#全栈开发岗位)
  - [游戏开发岗](#游戏开发岗位)
- [二、简历中的技能关键词](#二简历中的技能关键词)
- [三、面试高频问题与参考答案](#三面试高频问题与参考答案)
  - [基础问题（必问）](#一基础问题必问)
  - [Java / Spring Boot 进阶](#二java--spring-boot-进阶)
  - [Redis 专项](#三redis-专项)
  - [数据库 / MyBatis-Plus](#四数据库--mybatis-plus)
  - [系统设计 / 架构](#五系统设计--架构)
  - [安全 / 认证](#六安全--认证)
  - [前端专项](#七前端专项)
  - [Godot 游戏专项](#八godot-游戏专项)
  - [项目通用](#九项目通用问题)
- [四、STAR 法则示例（6个场景）](#四star-法则示例6个场景)
- [五、技术深挖准备清单](#五技术深挖准备清单)

---

## 一、简历项目描述（按岗位定制）

### 项目基本信息

> **项目名称**: 像素幸存者 (Pixel Survivor)
> **项目时间**: 2025.09 — 2026.05
> **项目性质**: 毕业设计 / 个人独立开发
> **代码规模**: 后端 43 个 Java 源文件 + 前端 15 个 Vue/JS 文件 + 游戏 31 个 GDScript 脚本

---

### 后端开发岗位

**项目名称**: 像素幸存者 — 游戏后端服务系统

**项目描述**:
基于 Spring Boot 3.2.5 + Java 17 开发的游戏后端服务，为 Godot 4 游戏客户端和 Vue 3 管理后台统一提供 RESTful API。项目涵盖 22 个 API 接口、9 张数据库表，实现了 JWT 双 Token 认证、Redis 五场景缓存方案、Redisson 分布式锁、AOP 操作审计、固定窗口限流等功能。

**个人职责与成果**:
- 设计并实现 JWT 双 Token 认证体系（游戏端 2h / 管理端 8h），通过 3 个自定义拦截器链完成认证、授权与限流，token 类型硬隔离防止越权
- 基于 Redis 实现 5 种生产级场景：用户信息 Cache-Aside 缓存（TTL 30min，读未命中回源写回）、仪表盘数据缓存（TTL 5~10min）、Redisson 分布式锁（tryLock + 自动续期，用于云存档上传并发控制）、固定窗口 API 限流（Redis INCR 原子自增，超限返回 HTTP 429，Redis 不可用时 fail-open 降级放行）、在线用户 Set 追踪 + @Scheduled 定时清理
- 使用 Spring AOP + 自定义 @OperationLog 注解实现管理端操作全量审计，在 @Around 切面中自动捕获操作人、IP、请求参数、响应结果、耗时，通过 JdbcTemplate 直接 SQL 写入确保即使业务异常也能落库
- 设计 JSON Blob 游戏存档同步方案：将全部游戏进度（角色、地图、成就、货币等）序列化为单条 JSON 存储在 `t_user_save_data` 表，通过 Jackson 序列化/反序列化，支持管理端在线编辑存档
- 使用 BCrypt（cost factor=10）加密存储所有密码，CSRF 禁用 + 无状态 Session 适配 RESTful 架构
- 采用全局异常处理器（@RestControllerAdvice）统一拦截 BusinessException、参数校验异常、未知异常，转换为标准 Result 响应格式

**技术栈**: Java 17, Spring Boot 3.2.5, MyBatis-Plus 3.5.5, MySQL 8.0, Redis 7.x (Lettuce + Redisson 3.27.2), Spring Security 6.x, JWT (jjwt 0.12.5), Spring AOP, Knife4j 4.4.0

---

### 全栈开发岗位

**项目名称**: 像素幸存者 — 全栈游戏平台

**项目描述**:
独立开发了包含 Godot 4 游戏客户端、Spring Boot 后端 API 服务、Vue 3 管理后台的完整游戏平台。后端提供 22 个 RESTful 接口，管理后台覆盖仪表盘、用户管理、存档编辑、操作日志等 6 个功能页面，游戏客户端实现完整的 Roguelike 幸存者玩法并通过 HTTP 与后端通信。

**个人职责与成果**:
- **后端**: 独立设计数据库（9 张表），实现 JWT 双 Token 认证、Redis 多场景缓存与限流、Redisson 分布式锁、AOP 日志审计等，所有接口提供 Knife4j 在线文档
- **前端**: 使用 Vue 3 Composition API + Vite + Element Plus + Pinia 开发管理后台，Login.vue 使用 Canvas 2D 实现黑洞引力透镜粒子动画（800 星点 + 200 吸积盘粒子 + 事件视界辉光），Dashboard.vue 使用 ECharts 实现用户增长趋势图（LinearGradient 渐变 + 暗色主题），UserDataManage.vue 实现游戏存档 JSON 的结构化编辑
- **前后端通信**: Axios 拦截器统一注入 JWT Bearer Token、自动拦截 401 跳转登录页；Vue Router beforeEach 路由守卫实现基于 Token 存在性的访问控制
- **游戏端**: Godot 4 GDScript 开发完整 Roguelike 游戏 —— 9 角色 / 16 武器 / 16 被动道具 / 4 敌人 / 6 Boss / 4 地图，通过 NetworkManager 单例与后端 HTTP API 对接实现登录注册 + 云存档同步

**技术栈**: Vue 3.4, Vite 5, Element Plus 2.5, Pinia 2, ECharts 6, Axios 1.6, Vue Router 4 | Java 17, Spring Boot 3.2.5, MyBatis-Plus 3.5.5, MySQL 8.0, Redis, Redisson, JWT | Godot 4, GDScript

---

### 游戏开发岗位

**项目名称**: 像素幸存者 — Roguelike 类幸存者游戏

**项目描述**:
使用 Godot 4 引擎独立开发的像素风 Roguelike 类幸存者游戏，受《Vampire Survivors》启发。实现了角色成长、武器升级、被动道具叠加、Boss 多阶段战斗、地图线性解锁、云存档同步等完整游戏循环。所有游戏数据（角色、武器、道具、敌人、Boss、地图、对话）通过 `database.gd` 数据驱动管理。

**个人职责与成果**:
- 设计并实现 16 种武器的数据驱动升级系统：每种武器 8 个等级，涵盖子弹（pistol/sniper）、近战（axe/melee_attack）、弹跳（baseball）、火焰（flamethrower/fireroad）、追踪（drone/missile）、环绕（talisman）、光束（orbital）、脉冲（pulse）、随机爆炸（matrix）、圣水（holywater）、星星（star）等多种攻击模式，每种武器的视觉通过 Image.create 程序化生成
- 实现 9 个可玩角色 + 6 个 Boss 多阶段战斗（1~3 阶段，含专属 BGM 和对话框），Boss 战流程：清理小怪 → 黑屏传送 → 3 秒 Boss 出场动画 → 多阶段战斗 → 吸收掉落物 → 胜利结算
- 16 种被动道具随机池系统：升级时从可用武器/道具中随机出 3~4 个选项，通过属性叠加（match stat 模式）动态计算角色最终属性（HP、护甲、伤害倍率、冷却、暴击、幸运、成长、贪婪、磁铁范围、移速、范围、持续时间、额外数量、生命恢复、复活次数）
- 地图系统：4 张地图线性解锁（教程 → 公路 → 荒原 → 深红森林），每张地图配置不同敌人组合和 Boss，难度随时间自动升档（`1.0 + game_time / 300.0`）
- GameManager 状态机管理游戏流程（MENU → PLAYING → PAUSED → LEVEL_UP → DIALOGUE → GAME_OVER / VICTORY），Boss 阶段通过信号系统（6 个信号）驱动 UI 更新
- 音频系统：BGM 支持交叉淡入淡出（crossfade），SFX 80ms 节流防重叠，20+ 音效文件，独立音量控制
- 云存档同步：NetworkManager 封装 HTTP 请求，通过后端 API 实现多设备游戏进度同步

**技术栈**: Godot 4, GDScript, HTTP (存档同步 REST API), AudioServer

---

## 二、简历中的技能关键词

根据个人情况和投递岗位，从以下关键词中选取合适的放入简历的「专业技能」栏：

```
后端: Java 17, Spring Boot 3, MyBatis-Plus, MySQL, Redis, Redisson
安全: Spring Security, JWT (jjwt), BCrypt, 自定义拦截器
缓存: Cache-Aside 模式, Redis String/Hash/Set, 缓存降级
并发: Redisson 分布式锁 (tryLock), @Transactional 事务
限流: 固定窗口算法, Redis INCR 原子自增, fail-open 降级
AOP:  自定义注解 @OperationLog, @Around 环绕通知, 操作审计
前端: Vue 3 Composition API, Vite, Element Plus, Pinia, ECharts, Axios
工程: RESTful API 设计, 全局异常处理, 统一响应体, Knife4j 文档
游戏: Godot 4, GDScript, 数据驱动设计, 状态机模式, 信号机制
```

---

## 三、面试高频问题与参考答案

---

### 一、基础问题（必问）

#### Q1: 请简要介绍一下这个项目？

**参考回答**: 这是一个像素风 Roguelike 类幸存者游戏的全栈项目，分为三层：Godot 4 游戏客户端、Spring Boot 后端服务、Vue 3 管理后台。后端提供 22 个 RESTful API，负责用户认证、游戏存档同步和管理端操作。管理后台基于 Element Plus，包含数据看板、用户管理、存档编辑、操作日志等功能。游戏客户端用 GDScript 编写，实现了完整的 Roguelike 玩法 —— 9 个角色、16 种武器和被动道具、6 个 Boss、4 张地图。

技术亮点是真正用上了 Redis 的多种场景（缓存、分布式锁、限流、在线追踪），以及 AOP 操作审计、JWT 双 Token 认证等企业级方案。

#### Q2: 你做这个项目的动机是什么？

**参考回答**: 这是我的毕业设计。选题时想做一个能综合运用大学所学技术的项目 —— 既要有后端（我的主方向），也要有前端和客户端。幸存者类游戏玩法简单但系统深度足够（武器升级、被动叠加、Boss 战），很适合作为练手项目。同时我刻意加入了 Redis 缓存、分布式锁、限流、AOP 日志这些在实际工作中才会用到的技术，目的是让项目更有面试竞争力。

#### Q3: 项目中你最有成就感的部分是什么？

**参考回答**: 最有成就感的是 Redis 限流功能的实现。这个功能从设计到落地涉及多个环节：自定义 @RateLimit 注解支持动态配置窗口和阈值、编写拦截器实现固定窗口算法（Redis INCR + EXPIRE 实现原子计数）、考虑 Redis 宕机时 fail-open 降级而不是拒绝所有请求、返回 HTTP 429 状态码和友好提示信息。这种「把一个问题想清楚并完整落地」的过程让我很有收获。

---

### 二、Java / Spring Boot 进阶

#### Q4: Spring Boot 的自动配置原理是什么？

**参考回答**: Spring Boot 通过 `@EnableAutoConfiguration` 注解触发自动配置。核心流程：

1. `@SpringBootApplication` 包含了 `@EnableAutoConfiguration`
2. `@EnableAutoConfiguration` 通过 `@Import(AutoConfigurationImportSelector.class)` 导入配置选择器
3. 选择器扫描 classpath 下所有 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` 文件（Spring Boot 3.x 新机制，旧版是 `spring.factories`）
4. 找到所有自动配置类后，通过 `@ConditionalOnClass`、`@ConditionalOnMissingBean`、`@ConditionalOnProperty` 等条件注解按需加载

举例：项目中引入 `spring-boot-starter-data-redis` 后，`RedisAutoConfiguration` 自动创建 `RedisTemplate` 和 `StringRedisTemplate` Bean。我自定义了 `RedisTemplate<String, Object>` 覆盖默认配置，使用 Jackson2JsonRedisSerializer 替换默认的 JdkSerializationRedisSerializer。

#### Q5: Spring 的拦截器 (Interceptor) 和过滤器 (Filter) 有什么区别？

**参考回答**:

| 维度 | Filter | Interceptor |
|------|--------|-------------|
| 规范 | Servlet 标准 | Spring MVC 特有 |
| 容器 | Servlet 容器管理 | Spring IoC 容器管理 |
| 范围 | 所有请求（包括静态资源） | 仅 DispatcherServlet 处理的请求 |
| 能力 | 只能操作 Request/Response | 可访问 Handler（Controller 方法）、参数 |
| 执行顺序 | Filter → Interceptor → Controller |

项目中 3 个拦截器（RateLimit → GameAuth → AdminAuth）都在 WebConfig 中注册。选择 Interceptor 因为需要根据请求路径区分认证策略（`/api/game/**` vs `/api/admin/**`），并且需要在 preHandle 中注入 userId/adminId 到 request attribute —— 这是 Interceptor 的典型场景。

#### Q6: `@Transactional` 的原理和失效场景？

**参考回答**: Spring 通过 AOP 代理实现事务管理。调用 `@Transactional` 方法时，代理对象在方法执行前通过 `TransactionManager` 开启事务，方法正常返回则提交，抛异常则回滚。

常见失效场景：
1. **同类方法调用**: 在同一个类中，方法 A 直接调用方法 B（this.methodB()），不会经过代理，事务不生效
2. **非 public 方法**: Spring AOP 默认基于 JDK/CGLIB 代理，只能拦截 public 方法
3. **异常被 catch**: 方法内部 try-catch 了异常且未重新抛出，事务管理器感知不到异常，不回滚
4. **非受检异常**: 默认只回滚 RuntimeException 和 Error，checked exception 不回滚（可通过 `rollbackFor = Exception.class` 解决）

项目中 `SyncServiceImpl.upload()` 标注了 `@Transactional(rollbackFor = Exception.class)`，确保任何异常都回滚，配合 Redisson 分布式锁保证并发安全。

#### Q7: Bean 的生命周期是怎样的？

**参考回答**:

```
实例化 (Constructor)
  → 属性注入 (@Autowired, @Value)
  → Aware 接口回调 (BeanNameAware, ApplicationContextAware 等)
  → BeanPostProcessor.postProcessBeforeInitialization()
  → @PostConstruct 方法
  → InitializingBean.afterPropertiesSet()
  → init-method
  → BeanPostProcessor.postProcessAfterInitialization()
  → Bean 就绪，可使用
  → @PreDestroy 方法
  → DisposableBean.destroy()
  → destroy-method
  → 销毁
```

项目中 `OnlineUserCleanupTask` 使用 `@PostConstruct` 不是必需的（因为 @Scheduled 在 Bean 初始化后自动开始），但 `RedissonConfig` 中配置 `@Bean(destroyMethod = "shutdown")` 确保应用关闭时正确释放 Redisson 连接。

#### Q8: Spring IoC 容器中，单例 Bean 如何处理并发请求？

**参考回答**: Spring 默认的 singleton Bean 是整个容器中只有一个实例，所有请求共享这个实例。关键点：
- Controller、Service、Repository 这些 Bean 通常是**无状态**的（不存储请求级别的数据），所以多线程并发访问是安全的
- 请求级别的数据（如 userId）通过方法参数或 request attribute 传递，不存储在成员变量中
- 如果确实需要请求级别的 Bean，可以使用 `@RequestScope` 或 `@SessionScope`

项目中 `GameAuthInterceptor` 从 JWT 解析 userId 后存入 `request.setAttribute("userId", userId)`，Controller 通过 `@RequestAttribute Long userId` 获取 —— 这是标准的有状态数据传递方式，不依赖 Bean 的成员变量。

---

### 三、Redis 专项

#### Q9: 项目里 Redis 具体用在哪些地方？

**参考回答**: 5 种场景，每个场景用了不同的 Redis 特性：

**1. 用户信息缓存（Cache-Aside 模式）**
- 数据结构: `RedisTemplate<String, Object>` 存序列化后的 User 对象
- Key: `user:info:{userId}`
- 流程: 查缓存 → 命中返回 / 未命中查 MySQL → 回写缓存 (TTL 30min)
- 降级: Redis 抛异常 → 静默查 MySQL

**2. 仪表盘统计缓存**
- Key: `dashboard:overview`、`dashboard:daily:{range}`
- TTL: 5 分钟（概览）/ 10 分钟（每日统计）
- 原因: 仪表盘查询涉及 COUNT 聚合 + 按天分组，每次查 MySQL 成本较高，且数据不需要实时更新

**3. Redisson 分布式锁（存档上传）**
- Key: `lock:sync:upload:{userId}`
- 方式: `tryLock(3, 10, TimeUnit.SECONDS)` —— 等待 3 秒获取锁，持有最多 10 秒
- 原因: 同一玩家多设备同时上传可能写覆盖，用分布式锁串行化

**4. 固定窗口 API 限流**
- Key: `rate:api:{clientIp}`
- 算法: INCR 原子自增 → 首次调用设置 EXPIRE 窗口时间 → 超出阈值返回 429
- 降级: Redis 不可用 → 打印 warn 日志 → 放行请求（fail-open）
- 使用: `@RateLimit(window = 60, maxRequests = 20)` 标注在 Controller 方法上

**5. 在线用户追踪**
- 数据结构: Redis Set `user:online`
- 操作: 登录 SADD userId，每 5 分钟 @Scheduled 扫描 Set 对比 DB 的 is_online 字段，清理离线用户

#### Q10: Cache-Aside 模式的具体流程？为什么先删缓存还是先更新数据库？

**参考回答**: Cache-Aside 的核心流程：

**读**: 查缓存 → 命中返回 / 未命中查 DB → 回写缓存 → 返回

**写**: 更新 DB → 删除缓存（而非更新缓存）

为什么是「先更新 DB，再删缓存」而不是反过来？
- 如果先删缓存再更新 DB：删除缓存后、更新 DB 前，另一个请求查到旧数据并回写缓存 —— 缓存里永远是旧数据，直到下次删除
- 如果先更新 DB 再删缓存：更新 DB 后、删除缓存前，另一个请求读到旧缓存（短暂不一致），删除后下次请求就会读新数据 —— 不一致窗口短且最终一致

项目中写操作（updateCurrency、updateGameStats 等）就是「更新 DB → 删除缓存」模式。

#### Q11: Redisson 分布式锁的底层原理？

**参考回答**: Redisson 基于 Redis 的 Hash 数据结构 + Lua 脚本实现分布式锁：

1. **加锁**: 执行 Lua 脚本，`HSET lock_key thread_id 1` + `PEXPIRE lock_key 30000ms`，如果 key 已存在且 thread_id 匹配则重入计数 +1
2. **WatchDog 续期**: 加锁成功后启动后台定时任务，每 10 秒续期到 30 秒（防止业务执行超时锁被自动释放）
3. **解锁**: 执行 Lua 脚本，检查 thread_id 是否匹配（防止误解锁），匹配则重入计数 -1，计数为 0 则删除 key

相比 SETNX 的简单实现，Redisson 解决了三个核心问题：
- **锁续期**: SETNX 设置固定过期时间，业务超时锁自动释放导致并发问题；Redisson WatchDog 自动续期
- **可重入**: Hash 结构存 thread_id + 重入计数
- **原子性**: 所有操作通过 Lua 脚本在 Redis 服务端原子执行

项目中用于云存档上传场景：`tryLock(3, 10, TimeUnit.SECONDS)` 表示等待 3 秒、锁持有最多 10 秒。

#### Q12: 固定窗口限流的临界点问题是什么？怎么解决？

**参考回答**: 固定窗口算法在窗口重置的临界点存在突发流量问题。

举例：限制每分钟 100 次请求。恶意客户端可以在第 59 秒发送 100 次（合法），然后在第 61 秒（新窗口开始）再发送 100 次（合法）。这样在短短 2 秒内发出了 200 次请求，但算法认为合法。

解决方案：
- **滑动窗口**: 记录每次请求的时间戳，检查当前时间往前一个窗口内的请求数。实现更复杂但平滑度更高
- **令牌桶**: 固定速率放令牌，请求需要获取令牌。允许一定的突发（桶容量 > 1）

项目中选择固定窗口是因为实现简单（只需 Redis INCR + EXPIRE 两条命令），对学生项目来说足够，且在代码中明确注释了算法选型和缺陷。

#### Q13: 项目中 Redis 的缓存穿透、击穿、雪崩怎么处理？

**参考回答**:

| 问题 | 描述 | 本项目应对 |
|------|------|-----------|
| **缓存穿透** | 查询不存在的数据，缓存没有，每次查 DB | 项目中用户查询基于自增 ID，几乎不会查不存在的 key。如果真的需要防穿透，可以缓存 null 值（短 TTL）或布隆过滤器（RedisConstant 中预留了 `bloom:user:id` key） |
| **缓存击穿** | 热点数据缓存过期，大量请求同时打到 DB | 用户信息缓存的 TTL 是 30 分钟，分散过期。如果进一步优化，可以用互斥锁（分布式锁）让第一个请求重建缓存，其他请求等待 |
| **缓存雪崩** | 大量缓存同时过期，DB 压力瞬间增大 | TTL 不是同一时刻设置的（每次写入独立计算），所以天然错开了过期时间。且用户量不大（毕业设计规模），DB 能承受 |

补充：项目中所有 Redis 操作都包裹了 try-catch 降级处理，Redis 不可用时静默退化到直接查 MySQL，不影响核心功能。

---

### 四、数据库 / MyBatis-Plus

#### Q14: 为什么选择 MyBatis-Plus 而不是 JPA 或原生 MyBatis？

**参考回答**:

| 特性 | MyBatis-Plus | JPA/Hibernate | 原生 MyBatis |
|------|:---:|:---:|:---:|
| 学习成本 | 低（MyBatis 增强） | 高（JPA 规范 + Hibernate） | 中 |
| SQL 控制力 | 强（可写自定义 SQL） | 弱（自动生成 SQL 难调优） | 最强 |
| CRUD 模板代码 | 几乎为零 | 几乎为零 | 需要手写 |
| 复杂查询 | LambdaQueryWrapper 类型安全 | Criteria API / JPQL | XML SQL |

选择 MP 的核心原因：
1. **类型安全**: `wrapper.eq(User::getUsername, username)` 比字符串拼接 `where username = #{username}` 更安全，IDE 有补全提示
2. **减少模板**: ServiceImpl 继承 `ServiceImpl<Mapper, Entity>` 直接获得 save/update/getById/page 等方法
3. **灵活性**: 复杂查询（如操作日志的多条件组合筛选）可以写 XML SQL，简单查询用 LambdaQueryWrapper

#### Q15: 项目里有哪些数据库索引？为什么这样建？

**参考回答**:

| 表 | 索引 | 原因 |
|----|------|------|
| t_user.username | UNIQUE | 登录时根据用户名查询，必须唯一 |
| t_user_save_data.user_id | UNIQUE | 每个用户一条存档，按 userId 查询/更新 |
| t_user_character.(user_id, character_code) | UNIQUE 联合 | 查询用户已解锁角色，防重复 |
| t_admin.username | UNIQUE | 登录查询 + 唯一约束 |
| t_admin_operation_log.admin_username | INDEX | 日志按管理员筛选 |
| t_admin_operation_log.created_at | INDEX | 日志按时间排序 + 日期范围筛选 |
| t_character_definition.char_code | UNIQUE | 角色定义唯一标识 |
| t_map_definition.map_code | UNIQUE | 地图定义唯一标识 |
| t_shop_item.item_code | UNIQUE | 商品唯一标识 |

设计原则：WHERE 条件列 + JOIN 关联列 + 唯一约束列建索引。

#### Q16: 为什么游戏存档用 JSON Blob 而不是分表存储？

**参考回答**:

**优点**:
1. **灵活性强**: 游戏版本迭代时，存档结构可能变化（增加新角色、新成就字段），JSON 可以直接扩展而无需 ALTER TABLE
2. **读写简单**: 一条 SELECT 读取全部数据，一条 UPDATE 写入全部数据，不需要多表 JOIN
3. **管理端友好**: 管理后台可以直接展示/编辑整个 JSON，不用维护复杂的表单

**缺点**:
1. 无法对存档内的字段进行 SQL 查询和统计（如"有多少玩家解锁了角色 X"）
2. 数据量大时（单条 JSON 过 MB），传输和解析有性能开销

**权衡**: 对于毕业设计规模的项目，JSON Blob 的灵活性优势远大于其缺点。如果未来需要做数据分析，可以结合 MySQL 8.0 的 JSON 列类型和虚拟索引，或者引入 MongoDB。

---

### 五、系统设计 / 架构

#### Q17: 画一下项目的系统架构图？

**参考回答**（口头描述即可）:

```
┌─────────────────────┐    HTTP/JSON     ┌──────────────────────┐
│   Godot 4 游戏客户端  │ ◄──────────────► │   Spring Boot 后端    │
│   (GDScript)         │   游戏 API        │   (Java 17, 端口8080) │
│   NetworkManager     │   /api/game/**    │                      │
└─────────────────────┘                   │  ┌─ 拦截器链 ──────┐ │
                                           │  │ RateLimit (order0)│ │
┌─────────────────────┐    HTTP/JSON      │  │ GameAuth          │ │
│   Vue 3 管理后台     │ ◄──────────────► │  │ AdminAuth         │ │
│   (Element Plus)     │   管理 API        │  └─────────────────┘ │
│   Axios + Pinia      │   /api/admin/**   │                      │
└─────────────────────┘                   │  ┌─ 服务层 ────────┐ │
                                           │  │ UserService       │ │
                                           │  │ AdminService      │ │
                                           │  │ SyncService       │ │
                                           │  └─────────────────┘ │
                                           │                      │
                                           │  ┌─ 基础设施 ──────┐ │
                                           │  │ MySQL 8.0         │ │
                                           │  │ Redis 7.x         │ │
                                           │  │ Redisson          │ │
                                           │  │ JWT + BCrypt      │ │
                                           │  └─────────────────┘ │
                                           └──────────────────────┘
```

核心设计决策：
- **前后端分离**: 游戏客户端和管理后台都通过 HTTP + JSON 与后端通信
- **无状态**: JWT 替代 Session，REST API 设计，CSRF 禁用
- **分层架构**: Controller → Service → Mapper，Interceptor 负责认证和限流
- **降级策略**: Redis 所有操作都有 try-catch，异常时退化到数据库或放行

#### Q18: 如果游戏有 10 万 DAU，需要做哪些优化？

**参考回答**:

1. **数据库层**:
   - 读写分离（主库写、从库读），MyBatis-Plus 支持多数据源
   - 分库分表（用户表按 userId 哈希分片），ShardingSphere
   - 慢查询优化：给高频查询字段加索引，EXPLAIN 分析执行计划

2. **缓存层**:
   - Redis 集群（Cluster 模式），数据分片存储
   - 多级缓存：Caffeine 本地缓存 + Redis 分布式缓存（项目中 pom.xml 已有 Caffeine 依赖但未使用）
   - 热点数据预加载，冷数据 LRU 淘汰

3. **应用层**:
   - 水平扩展：Nginx 反向代理 + 多实例部署，无状态服务天然支持
   - 消息队列异步化：存档上传可以异步写入 MQ，削峰填谷
   - 连接池调优：HikariCP 最大连接数从 20 调整到（CPU核数 * 2 + 磁盘数）

4. **游戏端**:
   - CDN 分发客户端资源包
   - 差量同步：只上传变化的存档字段而非整个 JSON

5. **限流升级**:
   - 固定窗口升级为令牌桶算法（更平滑）
   - 在网关层（Nginx / Spring Cloud Gateway）做全局限流

#### Q19: 限流实现在项目中的位置？为什么在拦截器而不是网关？

**参考回答**: 限流在 `RateLimitInterceptor` 中实现，注入到 WebConfig 的拦截器链第一个位置（order 0）。

选择应用层限流而非网关层的原因：
1. 项目规模不需要引入额外的网关组件（Nginx/Spring Cloud Gateway）
2. 应用层限流可以按 IP 粒度精确控制，且能针对不同接口设置不同的限流策略（`@RateLimit(window=60, maxRequests=20)`）
3. 使用 Redis 做计数存储，多个应用实例共享同一限流状态（为水平扩展做准备）

如果上线生产，理想的限流架构是：Nginx 限流（第一层防御，防 DDoS）→ 应用层限流（第二层，按业务接口细粒度控制）。

---

### 六、安全 / 认证

#### Q20: JWT 的结构是怎样的？签名验证如何防止篡改？

**参考回答**: JWT 由三部分组成，用 `.` 分隔：

```
Header.Payload.Signature
```

- **Header**: `{"alg": "HS256", "typ": "JWT"}` → Base64Url 编码
- **Payload**: `{"sub": userId, "username": "xxx", "tokenType": "game", "iat": 时间戳, "exp": 过期时间}` → Base64Url 编码
- **Signature**: `HMAC-SHA256(base64UrlEncode(Header) + "." + base64UrlEncode(Payload), secret)` → Base64Url 编码

**防篡改原理**: 攻击者修改 Payload 后重新 Base64Url 编码，但他不知道 secret，算不出正确的 Signature。服务端验证时用 secret 重新计算签名，不一致则拒绝。

**项目中**: 使用 jjwt 库，通过 `Jwts.parser().verifyWith(secretKey).build().parseSignedClaims(token)` 自动完成签名验证。secretKey 是配置在 application.yml 中的 HMAC 密钥。

#### Q21: 为什么设计了「游戏 Token」和「管理 Token」两种？

**参考回答**: 两种 Token 虽然结构相同，但 `tokenType` 字段不同（"game" vs "admin"），拦截器通过匹配规则实现硬隔离：

- `GameAuthInterceptor` 只处理 `/api/game/**` 路径，只接受 tokenType="game" 的 Token
- `AdminAuthInterceptor` 只处理 `/api/admin/**` 路径，只接受 tokenType="admin" 的 Token
- 过期时间不同：游戏 Token 2 小时，管理 Token 8 小时（管理员期望更长的登录有效期）
- 安全考虑：即使游戏端 Token 泄露，也无法访问管理端接口

这是一种简单有效的权限隔离方案。

#### Q22: Token 过期了怎么办？为什么不用 Refresh Token？

**参考回答**: 当前设计是最简单的过期重登方案：Token 过期后客户端收到 401，引导用户重新输入用户名密码登录。

**为什么没用 Refresh Token**: 毕业设计项目的并发量和安全威胁模型不需要 Refresh Token 的复杂度。生产环境下应该使用双 Token 机制：
- Access Token: 短期（15 分钟），每次请求携带
- Refresh Token: 长期（7 天），只在 Access Token 过期时使用，存储在后端可撤销

好处：即使 Access Token 泄露，窗口也只有 15 分钟；Refresh Token 可以主动撤销。

项目中 Interceptor 中已经预留了 Token 过期返回 401 的逻辑，前端 Axios 拦截器会自动处理 401 跳转登录页。

#### Q23: BCrypt 加密的特点？为什么不用 MD5/SHA256？

**参考回答**:

| 特性 | BCrypt | MD5 | SHA256 |
|------|:---:|:---:|:---:|
| 盐值 (Salt) | 内置自动生成 | 无，需手动加盐 | 无，需手动加盐 |
| 计算速度 | **慢**（故意） | 极快 | 快 |
| 抗彩虹表 | 内置 salt | 加盐后可以 | 加盐后可以 |
| 抗暴力破解 | **强**（慢哈希） | 弱（快哈希） | 弱（快哈希） |

MD5/SHA256 设计目标是快（用于校验文件完整性），而密码哈希需要慢（增加暴力破解成本）。BCrypt 的 cost factor=10 意味着 2^10=1024 轮哈希计算，每次登录多花几十毫秒，但暴力破解成本增加 1024 倍。

项目中 `PasswordEncoder` 接口注入的是 `BCryptPasswordEncoder(10)`，加密和验证都通过 Spring Security 的 API 完成。

---

### 七、前端专项

#### Q24: 管理后台的路由守卫是怎么实现的？

**参考回答**: 在 `router/index.js` 的 `beforeEach` 全局前置守卫中：

```javascript
router.beforeEach((to, from, next) => {
  const adminStore = useAdminStore()
  const token = adminStore.token  // 从 Pinia + localStorage 获取

  if (to.path === '/login') {
    // 已登录用户访问登录页 → 重定向到首页
    if (token) { next('/dashboard') }
    else { next() }
  } else {
    // 未登录用户访问任何页面 → 重定向到登录页
    if (!token) { next('/login') }
    else { next() }
  }
})
```

逻辑很简单：
- 有 Token → 允许访问任何页面
- 没有 Token → 只能访问登录页

这是基于 Token 存在性的简单守卫，没有做角色/权限区分（毕业设计只有一个管理员角色）。

#### Q25: Axios 拦截器统一处理了什么？

**参考回答**:

**请求拦截器**:
- 自动从 Pinia Store 获取 JWT Token，添加到 `Authorization: Bearer xxx` 请求头
- 所有请求自动带上 Token，开发者不需要手动添加

**响应拦截器**:
- 检查 `response.data.code`，非 200 的 business code 统一用 `ElMessage.error()` 提示
- HTTP 401 状态码 → 清除 Token → 跳转 `/login`
- 网络错误（无响应）→ 统一提示"网络连接异常"

这样做的好处：每个 API 调用不用写重复的错误处理逻辑，所有接口的错误码/401/网络异常都在一个地方集中处理。

#### Q26: Login.vue 的黑洞动画是怎么实现的？

**参考回答**: 使用 HTML5 Canvas 2D API 在 `<canvas>` 元素上逐帧绘制，通过 `requestAnimationFrame` 驱动动画循环：

1. **星空层**: 800 个星点粒子，围绕黑洞中心沿椭圆轨道运行，越靠近黑洞速度越快
2. **引力透镜**: 背景星光经过黑洞附近时路径弯曲（`bendFactor` / `bendAngle` 计算），模拟广义相对论的引力透镜效应
3. **吸积盘**: 200 个粒子在黑洞赤道面形成旋转环，通过 `y * 0.3` 挤压模拟椭圆透视效果
4. **事件视界**: 中心黑色圆 + 3 层径向渐变辉光（暗红 → 亮橙，sinusoidal 脉冲呼吸效果），以及光子球边缘的白色细环

每帧绘制前不完全清除画布（`fillStyle = rgba(0, 0, 0, 0.15)`），残留 15% 的内容产生拖尾/余辉效果。

---

### 八、Godot 游戏专项

#### Q27: Godot 的信号机制是什么？和观察者模式的关系？

**参考回答**: Godot 的信号本质上是**观察者模式**（发布-订阅）的内置实现。对象发射信号（emit），其他对象连接（connect）该信号来监听。

项目中 GameManager 定义了 11 个信号，连接了游戏的核心流程：

```gdscript
signal boss_spawned(boss_name: String)
signal boss_defeated(boss_id: String)
signal boss_phase_started
signal boss_intro_started
signal boss_teleport_started(callback: Callable)
signal game_over(victory: bool)
signal player_leveled_up(new_level: int)
# ... 等
```

Boss 战流程就靠这些信号串联：
1. `boss_phase_started` → HUD 显示警告文字
2. `boss_teleport_started(callback)` → 黑屏动画 + 回调传送玩家 + 创建竞技场
3. `boss_intro_started` → 显示 Boss 名称/称号
4. `boss_spawned(name)` → 切换 Boss BGM + 显示 Boss 血条
5. Boss 死亡后 `boss_defeated(id)` → 清理 → 吸收掉落物

信号的好处：GameManager 不需要知道谁在监听，HUD/game_scene 各管各的 UI 更新，解耦很干净。

#### Q28: 游戏的数据驱动设计怎么实现的？

**参考回答**: 所有游戏数据（角色、武器、被动、敌人、Boss、地图、对话、升级经验表）都集中定义在 `database.gd` 的 Dictionary 中，不硬编码在任何游戏逻辑脚本里。

举例：角色的基础属性定义为：
```gdscript
"maphy": {
    "name": "玛菲", "name_en": "Maphy",
    "max_hp": 100, "speed": 200, "armor": 0,
    "damage_mult": 1.0, "cooldown_mult": 1.0,
    "starting_weapon": "pistol", "passive": "",
    "unlock_cost": 0,
}
```

GameManager 在 `start_game()` 时从 database 读取所选角色的属性来初始化玩家状态。

好处：
- 数值调整不需要改代码，只改 dictionary
- 新增角色/武器只需加一条数据
- 如果将来接后端，可以直接用 JSON 替换 dictionary

#### Q29: Godot 4 中 GDScript 和 C# 的区别？为什么选 GDScript？

**参考回答**:

| 维度 | GDScript | C# |
|------|----------|-----|
| 语法 | Python-like，缩进敏感 | C# 标准语法 |
| 学习曲线 | 低（Python 用户秒上手） | 中（需要了解 .NET 生态） |
| 性能 | 脚本语言，较慢 | JIT/AOT 编译，快 |
| Godot 集成 | 原生支持、API 最完整 | 通过 .NET 绑定 |
| 编译依赖 | 无 | 需要 .NET SDK |
| 适合场景 | 中小型项目、原型 | 大型项目、计算密集型 |

选择 GDScript 的原因：
1. 幸存者类游戏的性能瓶颈在渲染而非脚本（移动端、GPU），GDScript 够用
2. 项目规模（31 个脚本文件）不需要强类型语言的优势
3. Godot 4 的 GDScript 2.0 已支持可选类型注解（`var player_hp: int = 100`），代码可读性足够
4. 开发效率高，不需要编译，修改即生效

---

### 九、项目通用问题

#### Q30: 项目中遇到的最大挑战是什么？怎么解决的？

**参考回答**: 最大挑战是实现 Redis 限流的「fail-open 降级」。一开始限流拦截器的逻辑很简单：Redis INCR 计数，超出就拒绝。但后来想到一个问题 —— 如果 Redis 挂了怎么办？是不是所有请求都会被拒绝？

解决方案：在拦截器的 Redis 调用外层包裹 try-catch，捕获所有异常后：
- 打印 warn 日志记录异常信息
- 调用 `return true`（放行请求）
- 不向客户端暴露任何 Redis 故障信息

这样确保了「宁可失去限流保护，也不能影响正常业务」——这是一个权衡：对毕业设计来说，可用性比限流精度更重要。

设计这个降级策略的过程让我学会了：写代码不仅要考虑 happy path，还要考虑基础设施故障时的退化行为。

#### Q31: 如果重新做这个项目，会做哪些改进？

**参考回答**:

1. **引入单元测试和集成测试**: 当前项目没有任何自动化测试，这是最大的遗憾。应该用 JUnit 5 + Mockito 写 Service 层单元测试，用 Testcontainers 写集成测试（真实 MySQL + Redis）
2. **Refresh Token 机制**: 当前的 JWT 过期重登方案太粗暴，应该实现双 Token 无感刷新
3. **API 版本管理**: 接口路径应该加版本号（如 `/api/v1/game/...`），做前后端兼容
4. **日志体系**: 当前只有简单的 println 和 @Slf4j 打印，应该接入 ELK（Elasticsearch + Logstash + Kibana）做集中日志收集
5. **监控告警**: 接入 Prometheus + Grafana，监控 QPS、延迟、错误率、Redis 命中率等关键指标
6. **前端状态管理**: Pinia Store 目前只存了 token 和 admin info，用户列表、操作日志等都没做缓存，每次切页面都重新请求
7. **游戏端离线模式**: 当前游戏强依赖后端登录，应该支持离线游玩 + 登录后自动云同步

#### Q32: 你提到了很多「降级策略」，能具体总结一下项目中所有的降级点吗？

**参考回答**:

| 组件 | 操作 | 降级行为 |
|------|------|----------|
| Redis 缓存读取 | 读缓存异常 | 记录 warn 日志，查 MySQL |
| Redis 缓存写入 | 写缓存异常 | 记录 warn 日志，静默跳过 |
| Redis 缓存删除 | 删缓存异常 | 记录 warn 日志，静默跳过 |
| Redis 限流检查 | INCR 异常 | 记录 warn 日志，放行请求 |
| Redis 在线用户 | SADD 异常 | 记录 warn 日志，跳过 |
| 分布式锁 | 获取锁失败 | 抛 BusinessException，不降级（数据一致性风险大） |
| Jackson JSON 解析 | 解析失败 | 返回 null/默认值，客户端自行处理 |

核心原则：**可降级的降级，不可降级的报错**。分布式锁是唯一不可降级的，因为它保护的是数据一致性 —— 降级可能导致数据损坏，比拒绝服务更严重。

---

## 四、STAR 法则示例（6个场景）

### 场景 1: Redis 多场景应用

**S (情境)**: 项目后端最初对 Redis 的使用非常基础 —— 只是在写操作后删除缓存（缓存失效），做单薄的缓存一致性维护。

**T (任务)**: 提升 Redis 在项目中的应用深度，从单一缓存失效拓展到多种生产级场景，增强项目的技术含金量。

**A (行动)**:
1. 用户信息缓存：实现完整的 Cache-Aside 模式 —— 读时先查 Redis → 未命中查 MySQL → 回写 Redis（TTL 30min），写时更新 MySQL → 删除 Redis 缓存
2. 仪表盘缓存：统计查询（COUNT + 分组聚合）结果缓存 5~10 分钟，避免每次打开页面都查一次全表
3. Redisson 分布式锁：在云存档上传接口中，用 `tryLock(userId)` 防止同一玩家多设备并发写覆盖
4. 固定窗口限流：自定义 `@RateLimit` 注解 + 拦截器，用 Redis INCR 原子自增实现按 IP 计数，超出返回 HTTP 429，Redis 不可用时 fail-open 降级放行
5. 在线用户追踪：登录时 SADD 到 Redis Set，@Scheduled 定时任务每 5 分钟对比 DB 清理离线记录

**R (结果)**: Redis 从单一的缓存失效工具拓展为项目的基础设施核心，覆盖缓存、并发控制、限流、状态管理 4 大领域共计 5 个场景。每个场景都有完整的降级策略。

---

### 场景 2: AOP 操作日志

**S (情境)**: 管理后台需要记录所有管理员的操作（谁在什么时候做了什么、结果是什么），但不能在每个 Controller 方法里硬编码日志逻辑。

**T (任务)**: 实现一套无侵入的操作日志方案。

**A (行动)**:
1. 自定义 `@OperationLog` 注解（module / operation / description）
2. 编写 `OperationLogAspect` 切面类，`@Around("@annotation(operationLog)")` 拦截标记方法
3. 从 JWT 解析 adminId, adminUsername；从 HttpServletRequest 获取客户端真实 IP（X-Forwarded-For → X-Real-IP → RemoteAddr 优先级链）
4. 用 Jackson 序列化请求参数和响应结果（截断至 1000 字符，防大 JSON 撑爆字段）
5. 在 finally 块中通过 `JdbcTemplate` 直接执行 INSERT SQL 写入 `t_admin_operation_log` 表 —— 确保即使业务方法抛异常，日志也能写入

**R (结果)**: Controller 方法只需加一个 `@OperationLog` 注解即可自动记录操作日志。管理后台的「操作日志」页面可以按管理员、模块、日期范围筛选查询。

---

### 场景 3: JWT 双 Token 认证

**S (情境)**: 游戏客户端和管理后台都需要认证，但安全需求不同（游戏 Token 泄露的危害相对小，管理 Token 泄露危害大）。

**T (任务)**: 设计一套认证方案，区分游戏用户和管理员的认证，做到权限隔离。

**A (行动)**:
1. JWT Payload 中增加 `tokenType` 字段（"game" / "admin"）
2. 编写 GameAuthInterceptor：处理 `/api/game/**` 请求，只接受 tokenType="game" 的 Token，从 JWT 解析 userId 注入 request attribute
3. 编写 AdminAuthInterceptor：处理 `/api/admin/**` 请求，只接受 tokenType="admin" 的 Token，解析 adminId
4. 不同过期时间：游戏 Token 2h / 管理 Token 8h
5. 拦截器在 WebConfig 中注册，配置路径排除规则（登录/注册接口不拦截）

**R (结果)**: 游戏端和管理端的认证完全隔离。即使攻击者拿到游戏 Token，也无法访问任何管理端接口。

---

### 场景 4: JSON Blob 游戏存档同步

**S (情境)**: 游戏存档包含角色解锁、等级、货币、成就、地图进度等大量字段。如果用关系型方式（每个字段一列），存档结构变化时需要频繁 ALTER TABLE。

**T (任务)**: 设计一个灵活的存档存储方案。

**A (行动)**:
1. 用 `t_user_save_data` 表的 `save_data` LONGTEXT 列存储完整 JSON
2. 客户端上传时将整个存档序列化为 JSON → 服务端 Jackson 反序列化为 Map → upsert 到数据库
3. 管理端编辑时：先解析 JSON → 展示结构化表单（角色、地图、货币各一个区域）→ 编辑后合并回 JSON → 序列化存回数据库
4. 用 Redisson 分布式锁（`lock:sync:upload:{userId}`）防止并发上传写覆盖
5. 上传操作包裹 `@Transactional`，写入失败自动回滚

**R (结果)**: 一条记录承载全部存档数据，前后端解析简单，管理端编辑直接修改 JSON。新增游戏功能时不需要改表结构。

---

### 场景 5: 固定窗口限流

**S (情境)**: 项目的公开 API（登录、注册、存档上传）没有任何限流保护，可能被恶意脚本或意外高频调用打垮。

**T (任务)**: 实现一套轻量级、可配置的 API 限流方案。

**A (行动)**:
1. 选择固定窗口算法（Redis INCR + EXPIRE），实现简单且原子操作保证
2. 设计 `@RateLimit(window, maxRequests)` 注解
3. 编写 RateLimitInterceptor：从注解读取配置 → Redis key = `rate:api:{clientIp}` → INCR → 首次则 EXPIRE → 超限返回 HTTP 429
4. 全局降级策略：Redis 调用 try-catch，异常时打印 warn 日志并放行（fail-open）
5. 在 GameSyncController.upload() 和 GameUserController.login() 上标注不同的限流参数

**R (结果)**: API 有基本的防刷保护。限流策略可配置（不同接口不同阈值），Redis 故障时不影响业务。

---

### 场景 6: 用户数据管理编辑

**S (情境)**: 管理后台需要能够查看和编辑任意玩家的游戏存档。

**T (任务)**: 实现一个支持模糊查询、结构化展示、增量合并更新的存档编辑器。

**A (行动)**:
1. 前端支持按用户名搜索，后端返回用户基本信息 + 解析后的存档 JSON Map
2. 编辑表单按存档字段分组展示（货币、已解锁角色 + 等级、已解锁/已完成地图、成就）
3. 额外查询 `t_character_definition` 和 `t_map_definition` 提供下拉选项和数据对照（角色名、地图名）
4. 提交时只传修改的字段，后端合并现有 JSON（未传的字段保留原值），不影响其他存档数据
5. 完整的错误处理：用户不存在提示、JSON 序列化失败回滚

**R (结果)**: 管理员可以安全地编辑任意玩家的存档，不会丢失未修改的数据。

---

## 五、技术深挖准备清单

以下问题如果面试中写了这个项目，**大概率会被追问**。确保每个都能顺利回答：

### Java / Spring
- [ ] `@Autowired` 和构造器注入的区别？为什么 Spring 推荐构造器注入？
- [ ] Spring AOP 的两种代理方式（JDK 动态代理 vs CGLIB）分别在什么情况下使用？
- [ ] `BeanFactory` 和 `ApplicationContext` 的区别？
- [ ] Spring Boot 的 Starter 机制是什么？
- [ ] `@RestController` 和 `@Controller` 的区别？
- [ ] HTTP 请求从进入 Tomcat 到到达 Controller 方法的完整流程？
- [ ] `@Transactional` 的 7 种传播行为？

### Redis
- [ ] Redis 的 5 种基本数据类型 + 3 种扩展类型？各自的底层实现？
- [ ] Redis 的持久化方式（RDB vs AOF）？各自的优缺点？
- [ ] Redis 的内存淘汰策略（8 种）？
- [ ] Redis 主从复制 + 哨兵模式 vs Cluster 集群模式？
- [ ] 缓存和数据库的双写一致性？延迟双删是什么？
- [ ] Redisson 分布式锁的看门狗机制？

### MySQL / MyBatis
- [ ] MySQL InnoDB 的索引结构（B+ 树）？为什么不用 B 树或哈希？
- [ ] 聚簇索引和非聚簇索引的区别？
- [ ] 覆盖索引、最左前缀原则？
- [ ] MyBatis 的 `#{}` 和 `${}` 的区别？（SQL 注入防护）
- [ ] MyBatis 的一级缓存和二级缓存？

### 网络 / 协议
- [ ] HTTP 和 HTTPS 的区别？SSL/TLS 握手流程？
- [ ] TCP 三次握手和四次挥手？为什么是三次不是两次？
- [ ] Cookie、Session、Token 的区别？
- [ ] 跨域 (CORS) 是什么？项目中如何配置的？

### 设计模式
- [ ] 项目中用到了哪些设计模式？（单例 - Autoload、观察者 - Signal、代理 - AOP、模板方法 - ServiceImpl）
- [ ] 单例模式的多种实现方式？双重检查锁为什么需要 volatile？
- [ ] 策略模式和工厂模式的区别？

### DevOps / 工具
- [ ] Git 的工作流程？（feature branch → PR → merge）
- [ ] Maven 的生命周期？（clean → compile → test → package → install → deploy）
- [ ] Docker 的基本命令和使用？
- [ ] CI/CD 的基本概念？

---

## 附录: 快速自检表

面试前 30 分钟，快速过一遍：

1. [ ] 能用 30 秒讲清楚项目是什么（一句话概述）
2. [ ] 能画出系统架构图（3 层 + Redis + MySQL）
3. [ ] 能说出 22 个 API 接口的大致分类（4 游戏 + 2 同步 + 16 管理）
4. [ ] 能说出 9 张数据库表的名字和用途
5. [ ] 能说出 Redis 的 5 种使用场景及降级策略
6. [ ] 能说出 3 个拦截器的顺序和作用
7. [ ] 能说出 JWT 的三部分结构和签名验证原理
8. [ ] 能说出 AOP 操作日志的完整实现流程
9. [ ] 准备好 2~3 个 STAR 法则场景
10. [ ] 能回答「如果重新做会怎么改进」
