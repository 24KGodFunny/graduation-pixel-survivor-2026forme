# 像素幸存者 (Pixel Survivor) — Java后端实习面试完全指南

---

## 目录

- [一、简历项目描述](#一简历项目描述)
- [二、个人介绍话术](#二个人介绍话术)
- [三、每个功能的技术选型详解](#三每个功能的技术选型详解)
- [四、面试高频八股 + 场景题](#四面试高频八股--场景题)
- [五、高并发场景题专项](#五高并发场景题专项)
- [六、多余文件清单（可删除）](#六多余文件清单可删除)

---

## 一、简历项目描述

### 简历上的写法（控制在 8~12 行）

```
项目名称：像素幸存者 — 游戏后端服务系统（毕业设计）
项目时间：2025.09 — 2026.05
技术栈：Java 17 / Spring Boot 3.2.5 / MyBatis-Plus / MySQL 8.0 / Redis 7.x / Redisson / Vue 3

项目描述：
  为 Roguelike 类幸存者游戏（Godot 4 客户端）和 Vue 3 管理后台提供统一的 RESTful API
  服务，包含 22 个接口、9 张数据表。

个人职责：
  1. 设计 JWT 双 Token 认证体系（游戏端 2h / 管理端 8h），通过 3 个自定义拦截器链
     （限流→游戏认证→管理认证）实现按路径隔离的认证授权，Token 类型硬隔离防越权
  2. 基于 Redis 实现 5 种生产级场景：Cache-Aside 用户缓存（TTL 30min）、仪表盘
     数据缓存（TTL 5~10min）、Redisson 分布式锁（存档并发上传控制）、固定窗口
     API 限流（INCR+EXPIRE，fail-open 降级）、在线用户 Set 追踪+定时清理
  3. 使用 Spring AOP + 自定义 @OperationLog 注解实现管理端操作全量审计，@Around
     切面自动捕获操作人/IP/参数/响应/耗时，JdbcTemplate 直写 SQL 确保落库
  4. 设计 JSON Blob 云存档同步方案，将游戏进度序列化为单条 JSON 存储，支持
     Jackson 序列化/反序列化 + Redisson 分布式锁防并发写覆盖
  5. 全局异常处理器（@RestControllerAdvice）统一拦截异常，转换为标准 Result 响应；
     BCrypt 加密存储密码，Spring Security 配置无状态 Session + CSRF 禁用
```

### 关键数字（面试时随口说出）

| 指标 | 数据 |
|------|------|
| API 接口数 | 22 个 |
| 数据库表 | 9 张 |
| Redis 使用场景 | 5 种 |
| 自定义拦截器 | 3 个 |
| 自定义注解 | 2 个（@RateLimit, @OperationLog） |
| Java 源文件 | 42 个 |

---

## 二、个人介绍话术

### 30 秒版本（电梯演讲）

> 面试官您好，我叫 XXX，XX 大学计算机科学与技术专业 2026 届毕业生。
>
> 我最近的项目是一个游戏后端服务系统，用 Spring Boot 3 为 Godot 游戏客户端和 Vue
> 管理后台提供 RESTful API。技术上我重点做了五件事：JWT 双 Token 认证、Redis 五
> 场景应用（缓存/分布式锁/限流/在线追踪）、AOP 操作审计、JSON Blob 云存档同步、
> 全局异常处理。这 22 个接口覆盖了用户注册登录、游戏存档上传下载、管理员 CRUD、
> 操作日志查询等功能。
>
> 我对 Redis 的多场景使用和 Spring AOP 这两块比较有心得，可以深入聊聊。

### 2 分钟版本（完整介绍）

> 面试官您好，我叫 XXX，XX 大学 2026 届计算机专业毕业生。
>
> 我做了一个叫"像素幸存者"的全栈项目，是毕业设计。项目分为三层：Godot 4 游戏
> 客户端、Spring Boot 后端、Vue 3 管理后台。我主要负责后端开发。
>
> 后端基于 Spring Boot 3.2.5 + MyBatis-Plus + MySQL 8.0 + Redis 7.x，提供了
> 22 个 RESTful API 接口。核心功能包括：
>
> **认证方面**，我设计了 JWT 双 Token 机制——游戏用户 Token 有效期 2 小时，
> 管理员 Token 8 小时，通过 Token 中的 type 字段做硬隔离。三个自定义拦截器按
> 优先级执行：先限流、再分别做游戏端和管理端的认证。
>
> **缓存方面**，Redis 用在了 5 个场景：用户信息的 Cache-Aside 缓存、仪表盘
> 统计数据缓存、Redisson 分布式锁控制存档并发上传、基于 INCR 的固定窗口
> API 限流、以及用 Set 做在线用户追踪配合定时任务清理。所有 Redis 操作都有
> try-catch 降级策略——Redis 挂了不会影响核心业务。
>
> **日志方面**，我用 Spring AOP 做了操作审计。给 Controller 方法加一个
> @OperationLog 注解，切面就自动记录谁在什么时候做了什么操作、请求参数、
> 响应结果、IP 地址和耗时，直接写入数据库。
>
> 这个项目让我对 Spring 的核心机制（IoC、AOP、拦截器）、Redis 多数据结构
> 的实战运用、以及 JWT 认证体系有了比较深入的理解。如果您有兴趣，我可以
> 展开聊任何一个技术点。

---

## 三、每个功能的技术选型详解

### 3.1 JWT 双 Token 认证

**技术选型**: jjwt 0.12.5 + 自定义拦截器（非 Spring Security Filter Chain）

**为什么这么选**:
- **jjwt 而不是自己实现**: JWT 的签名验证涉及 HMAC-SHA256 算法，自己实现容易出安全漏洞。jjwt 是 Java 社区最成熟的 JWT 库，API 简洁
- **拦截器而不是 Spring Security Filter Chain**: Spring Security 的 Filter Chain 太重，配置复杂。本项目只需要验证 Token 有效性 + 注入 userId，用 3 个 HandlerInterceptor 就够了。但保留 Spring Security 依赖是为了用 BCryptPasswordEncoder
- **双 Token 而不是单 Token**: 游戏用户和管理员的安全需求不同。游戏 Token 泄露只影响单个用户，管理 Token 泄露影响整个系统。通过 type 字段硬隔离，即使拿到游戏 Token 也访问不了管理接口

**代码位置**: [JwtUtil.java](pixel-survivor-game/backend/src/main/java/com/pixelsurvivor/common/util/JwtUtil.java)
- 第 33-35 行 `getSigningKey()`: 将密钥转为 HMAC-SHA256 的 SecretKey 对象
- 第 40-46 行 `generateGameToken()`: 生成游戏端 Token，payload 含 userId/username/type="game"
- 第 51-58 行 `generateAdminToken()`: 生成管理端 Token，payload 含 adminId/username/role/type="admin"
- 第 74-80 行 `parseToken()`: 解析并验证 Token 签名，签名不匹配直接抛异常

---

### 3.2 Redis 五场景应用

**技术选型**: Spring Data Redis (Lettuce 客户端) + Redisson 3.27.2

**为什么这么选**:
- **Lettuce 而不是 Jedis**: Lettuce 基于 Netty，线程安全，支持异步。Spring Boot 2.x 之后默认使用 Lettuce。Jedis 是线程不安全的，需要连接池
- **Redisson 而不是手写 SETNX**: Redisson 封装了分布式锁的 WatchDog 自动续期、可重入、Lua 脚本原子操作。手写 SETNX 需要自己处理续期和误删问题，容易出 bug
- **StringRedisTemplate + RedisTemplate 两个模板**: StringRedisTemplate 用于简单的 String 操作（INCR/SADD/EXPIRE），RedisTemplate 用于存 Java 对象（Jackson 序列化）

#### 场景 1: Cache-Aside 用户缓存

**代码位置**: [UserServiceImpl.java:127-154](pixel-survivor-game/backend/src/main/java/com/pixelsurvivor/service/impl/UserServiceImpl.java#L127-L154)

```java
// 第 131-135 行：先查 Redis 缓存
User cached = (User) redisTemplate.opsForValue().get(cacheKey);
if (cached != null) return cached;

// 第 141 行：缓存未命中，查 MySQL
User user = this.getById(userId);

// 第 148 行：回写缓存，TTL 30 分钟
redisTemplate.opsForValue().set(cacheKey, user, 30, TimeUnit.MINUTES);
```

**选型理由**: Cache-Aside 是最经典的缓存模式。读的时候先查缓存，没命中再查 DB 并回写；写的时候更新 DB 后删除缓存（不是更新缓存）。TTL 30 分钟保证即使删除缓存失败，数据也不会永远过期。

#### 场景 2: Redisson 分布式锁（存档上传）

**代码位置**: [SyncServiceImpl.java:58-75](pixel-survivor-game/backend/src/main/java/com/pixelsurvivor/service/impl/SyncServiceImpl.java#L58-L75)

```java
// 第 59-60 行：拼接锁 key，每个用户独立一把锁
String lockKey = RedisConstant.LOCK_SYNC_UPLOAD + userId;
RLock lock = redissonClient.getLock(lockKey);

// 第 62 行：尝试加锁，最多等 3 秒获取锁，锁持有最多 10 秒
if (lock.tryLock(3, 10, TimeUnit.SECONDS)) {
    try {
        doUpload(userId, data);  // 执行实际上传逻辑
    } finally {
        lock.unlock();  // 释放锁
    }
} else {
    throw new BusinessException(ResultCode.PARAM_ERROR, "同步操作过于频繁，请稍后再试");
}
```

**选型理由**: 同一用户可能在手机和电脑同时登录游戏并上传存档。没有锁的话，两次上传可能读到相同的旧数据然后各自写入，后写的覆盖先写的，数据丢失。Redisson 的 tryLock 支持等待超时（3 秒内拿不到锁就放弃），WatchDog 自动续期（如果业务超过 10 秒，锁不会被意外释放）。

#### 场景 3: 固定窗口 API 限流

**代码位置**: [RateLimitInterceptor.java:90-111](pixel-survivor-game/backend/src/main/java/com/pixelsurvivor/config/interceptor/RateLimitInterceptor.java#L90-L111)

```java
// 第 92 行：Redis INCR 原子自增（Redis 保证原子性）
Long count = stringRedisTemplate.opsForValue().increment(redisKey);

// 第 95-97 行：如果是第一次访问（count=1），设置过期时间
if (count != null && count == 1) {
    stringRedisTemplate.expire(redisKey, window, TimeUnit.SECONDS);
}

// 第 100-103 行：超出阈值，返回 429
if (count != null && count > maxRequests) {
    writeRateLimitResponse(response, window);
    return false;
}

// 第 105-108 行：Redis 异常时 fail-open 降级放行
} catch (Exception e) {
    log.warn("Redis 操作异常，限流拦截器降级放行: {}", e.getMessage());
    return true;
}
```

**选型理由**: 固定窗口是限流算法中最简单的实现——只需 Redis INCR + EXPIRE 两条命令。INCR 是 Redis 原子操作，不存在并发问题。选择 fail-open 而不是 fail-closed 是因为：对毕业设计项目来说，可用性比限流精度更重要。

#### 场景 4: 仪表盘数据缓存

**代码位置**: [AdminServiceImpl.java:319-375](pixel-survivor-game/backend/src/main/java/com/pixelsurvivor/service/impl/AdminServiceImpl.java#L319-L375)

```java
// 第 367-369 行：统计结果写入 Redis 缓存，TTL 5 分钟
redisTemplate.opsForValue().set(cacheKey, overview, 5, TimeUnit.MINUTES);
```

**选型理由**: 仪表盘需要 COUNT 聚合 + 按天分组统计，每次查询全表成本较高。管理员不需要实时数据，5 分钟的缓存足够。

#### 场景 5: 在线用户追踪

**代码位置**: [UserServiceImpl.java:107-111](pixel-survivor-game/backend/src/main/java/com/pixelsurvivor/service/impl/UserServiceImpl.java#L107-L111) + [OnlineUserCleanupTask.java:63](pixel-survivor-game/backend/src/main/java/com/pixelsurvivor/task/OnlineUserCleanupTask.java#L63)

```java
// UserServiceImpl.login() 第 108 行：登录时将 userId 加入 Redis Set
stringRedisTemplate.opsForSet().add(RedisConstant.USER_ONLINE, user.getId().toString());

// OnlineUserCleanupTask 第 63 行：每 5 分钟清理离线用户
@Scheduled(fixedRate = 300000)
public void cleanOfflineUsers() { ... }
```

**选型理由**: Redis Set 天然支持去重和集合操作。SADD 添加用户，定时任务遍历 Set 对比数据库 is_online 字段，不一致的就 SREM 移除。

---

### 3.3 AOP 操作日志

**技术选型**: Spring AOP + 自定义注解 + JdbcTemplate

**为什么这么选**:
- **AOP 而不是手动记录**: 如果在每个 Controller 方法里写日志代码，侵入性太强。AOP 只需加一个注解，切面自动处理
- **@Around 而不是 @After**: @Around 可以在方法执行前后都拿到控制权，能记录方法耗时（`System.currentTimeMillis()` 前后差值）和异常信息
- **JdbcTemplate 而不是 MyBatis Mapper**: 在 AOP 切面中注入 Mapper 可能有循环依赖风险（AOP 切面先于某些 Bean 初始化）。JdbcTemplate 是 Spring 原生提供的，注入更安全。而且日志写入是简单的 INSERT，不需要 MyBatis 的查询能力

**代码位置**: [OperationLogAspect.java:36-56](pixel-survivor-game/backend/src/main/java/com/pixelsurvivor/common/aspect/OperationLogAspect.java#L36-L56)

```java
// 第 36-37 行：@Around 拦截所有标注了 @OperationLog 的方法
@Around("@annotation(operationLog)")
public Object around(ProceedingJoinPoint joinPoint, OperationLog operationLog) throws Throwable {
    long startTime = System.currentTimeMillis();
    Object result = null;
    String errorMsg = null;
    try {
        result = joinPoint.proceed();  // 第 43 行：执行目标方法
        return result;
    } catch (Throwable e) {
        errorMsg = e.getMessage();     // 第 46 行：捕获异常信息
        throw e;                       // 继续抛出，不影响原方法行为
    } finally {
        long costTime = System.currentTimeMillis() - startTime;
        saveLog(...);                  // 第 51 行：finally 块保证日志一定写入
    }
}
```

**选型理由**: `finally` 块保证即使目标方法抛异常，日志也能记录（包含异常信息 errorMsg）。用 `JdbcTemplate.update()` 直接执行 INSERT SQL，不依赖 MyBatis 事务管理器，避免与业务事务互相影响。

---

### 3.4 Spring Security + BCrypt

**技术选型**: Spring Security 6.x（仅用于 BCryptPasswordEncoder）+ 自定义拦截器做认证

**为什么这么选**:
- **只用 Security 的密码编码器**: Spring Security 的整套 Filter Chain 太重，配置繁琐。本项目只需要 BCrypt 加密密码，所以 SecurityConfig 里禁用了 CSRF、Session、Form Login、HTTP Basic，把所有请求 permitAll，认证交给自定义拦截器
- **BCrypt 而不是 MD5/SHA256**: BCrypt 是慢哈希算法，内置 salt，cost factor 可调。MD5/SHA256 是快哈希，专门设计来快速计算，不适合密码场景

**代码位置**: [SecurityConfig.java:22-37](pixel-survivor-game/backend/src/main/java/com/pixelsurvivor/config/SecurityConfig.java#L22-L37)

```java
// 第 25-28 行：禁用 CSRF（REST API 无状态，不需要 CSRF Token）
.csrf(AbstractHttpConfigurer::disable)
// 第 27-28 行：禁用 Session（使用 JWT，不需要服务端 Session）
.sessionManagement(session -> session
    .sessionCreationPolicy(SessionCreationPolicy.STATELESS))
// 第 33-34 行：所有请求放行（认证由自定义拦截器处理）
.authorizeHttpRequests(auth -> auth.anyRequest().permitAll());
```

---

### 3.5 全局异常处理

**技术选型**: @RestControllerAdvice + 自定义 BusinessException

**为什么这么选**:
- **@RestControllerAdvice 而不是每个方法 try-catch**: 统一异常处理，Controller 只管抛异常，不需要关心错误响应格式
- **自定义 BusinessException 而不是用枚举直接抛**: BusinessException 封装了错误码和消息，GlobalExceptionHandler 统一转换为 `Result.error()`

**代码位置**: [GlobalExceptionHandler.java:25-62](pixel-survivor-game/backend/src/main/java/com/pixelsurvivor/common/exception/GlobalExceptionHandler.java#L25-L62)

```java
// 第 25-29 行：业务异常处理（已知的、可预期的异常）
@ExceptionHandler(BusinessException.class)
public Result<?> handleBusinessException(BusinessException e) {
    return Result.error(e.getCode(), e.getMessage());
}

// 第 31-39 行：参数校验异常（@Valid 验证失败）
@ExceptionHandler(MethodArgumentNotValidException.class)
public Result<?> handleValidationException(MethodArgumentNotValidException e) {
    // 拼接所有字段的错误信息
    String message = e.getBindingResult().getFieldErrors().stream()
            .map(error -> error.getField() + ": " + error.getDefaultMessage())
            .reduce((a, b) -> a + "; " + b).orElse("参数校验失败");
    return Result.error(ResultCode.BAD_REQUEST, message);
}

// 第 57-62 行：兜底——所有未预见的异常
@ExceptionHandler(Exception.class)
public Result<?> handleException(Exception e) {
    log.error("系统异常", e);  // 记录完整堆栈
    return Result.error(ResultCode.INTERNAL_ERROR);  // 不暴露内部信息给客户端
}
```

---

### 3.6 JSON Blob 云存档同步

**技术选型**: Jackson ObjectMapper + LONGTEXT 列 + Redisson 分布式锁

**为什么这么选**:
- **JSON Blob 而不是分表存储**: 游戏存档结构频繁变化（新增角色/武器/成就），JSON 可以灵活扩展而无需 ALTER TABLE。一条 SELECT 读全部，一条 UPDATE 写全部，不需要多表 JOIN
- **Jackson 而不是 Gson**: Jackson 是 Spring Boot 默认的 JSON 库，与 Spring 生态深度集成。性能略优于 Gson，支持更多注解配置
- **Redisson 锁**: 见上面场景 2 的解释

**代码位置**: [SyncServiceImpl.java:80-113](pixel-survivor-game/backend/src/main/java/com/pixelsurvivor/service/impl/SyncServiceImpl.java#L80-L113)

```java
// 第 83-84 行：将客户端上传的 Map 数据序列化为 JSON 字符串
jsonStr = objectMapper.writeValueAsString(data.getSaveData());

// 第 90-105 行：upsert 策略——有则更新，无则插入
UserSaveData existing = userSaveDataMapper.selectOne(wrapper);
if (existing != null) {
    existing.setSaveData(jsonStr);
    userSaveDataMapper.updateById(existing);
} else {
    UserSaveData newSave = new UserSaveData();
    newSave.setUserId(userId);
    newSave.setSaveData(jsonStr);
    userSaveDataMapper.insert(newSave);
}
```

---

### 3.7 统一响应体 Result<T>

**技术选型**: 泛型封装类 + 枚举错误码

**为什么这么选**:
- 前后端分离项目必须统一响应格式。所有接口返回 `Result<T>`，前端可以统一处理 code/message/data
- 枚举错误码比魔法数字好维护。每个错误码有中文消息，前端可以直接展示

**代码位置**: [Result.java](pixel-survivor-game/backend/src/main/java/com/pixelsurvivor/common/result/Result.java) + [ResultCode.java](pixel-survivor-game/backend/src/main/java/com/pixelsurvivor/common/result/ResultCode.java)

---

### 3.8 拦截器链设计

**技术选型**: Spring MVC HandlerInterceptor + WebMvcConfigurer 注册

**为什么这么选**:
- **拦截器顺序**: RateLimit(order=0) → GameAuth(order=1) → AdminAuth(order=2)。先限流，再认证。如果认证通过了但没限流，浪费资源
- **路径分离**: `/api/game/**` 用 GameAuthInterceptor，`/api/admin/**` 用 AdminAuthInterceptor。排除登录注册路径

**代码位置**: [WebConfig.java:39-58](pixel-survivor-game/backend/src/main/java/com/pixelsurvivor/config/WebConfig.java#L39-L58)

```java
// 第 41-43 行：限流拦截器优先级最高（order=0），对所有 /api/** 生效
registry.addInterceptor(rateLimitInterceptor)
        .addPathPatterns("/api/**")
        .order(0);

// 第 46-52 行：游戏端认证拦截器，排除登录注册
registry.addInterceptor(gameAuthInterceptor)
        .addPathPatterns("/api/game/**")
        .excludePathPatterns("/api/game/auth/**", "/api/game/user/login", "/api/game/user/register");

// 第 55-57 行：管理端认证拦截器，排除登录
registry.addInterceptor(adminAuthInterceptor)
        .addPathPatterns("/api/admin/**")
        .excludePathPatterns("/api/admin/auth/login");
```

---

### 3.9 MyBatis-Plus 选型

**技术选型**: MyBatis-Plus 3.5.5 而不是 JPA/Hibernate

**为什么这么选**:
- **类型安全**: `LambdaQueryWrapper<User>::eq(User::getUsername, username)` 比字符串拼写 `where username = #{username}` 安全，IDE 有自动补全
- **零 CRUD 模板**: ServiceImpl 继承后直接有 save/update/getById/page 方法
- **灵活**: 复杂查询可以写 XML SQL（如 UserItemMapper.xml），简单查询用 LambdaQueryWrapper
- 相比 JPA：JPA 的自动生成 SQL 难调优，复杂关联查询要用 Criteria API 或 JPQL，学习成本高

---

## 四、面试高频八股 + 场景题

### 4.1 Java 基础八股

#### Q: HashMap 底层原理？JDK 1.7 和 1.8 的区别？

**答**: HashMap 底层是数组+链表（1.7）或数组+链表+红黑树（1.8）。

- **1.7**: 头插法插入链表，并发扩容时可能形成环形链表导致死循环
- **1.8**: 尾插法，链表长度 >= 8 且数组长度 >= 64 时转红黑树（O(n) → O(logn)）。扩容时不需要重新哈希，只需判断高位是 0 还是 1
- **为什么用红黑树不用二叉搜索树**: 二叉搜索树可能退化为链表（O(n)），红黑树是自平衡的，最坏 O(logn)

**项目关联**: 本项目没有直接操作 HashMap，但 MyBatis-Plus 的 LambdaQueryWrapper 内部用 HashMap 缓存列信息。

---

#### Q: ConcurrentHashMap 原理？1.7 和 1.8 的区别？

**答**:
- **1.7**: Segment 分段锁（继承 ReentrantLock），每个 Segment 管理一段数据，不同 Segment 间可并发
- **1.8**: 废弃 Segment，用 CAS + synchronized 锁单个 Node。锁粒度更细（锁链表头节点或红黑树根节点），并发度更高

**项目关联**: 本项目是 Web 应用，每个请求是独立线程，Controller/Service 是无状态单例，不需要自己操作 ConcurrentHashMap。但如果做本地缓存（如 Caffeine），底层会涉及并发数据结构。

---

#### Q: synchronized 和 ReentrantLock 的区别？

**答**:

| 维度 | synchronized | ReentrantLock |
|------|-------------|---------------|
| 实现 | JVM 层面（monitorenter/exit） | JDK 层面（AQS） |
| 释放 | 自动释放（退出同步块） | 必须手动 unlock() |
| 可中断 | 不可中断 | lockInterruptibly() 可中断 |
| 公平锁 | 非公平 | 可选公平/非公平 |
| 条件变量 | 单一 wait/notify | 多个 Condition |
| 锁绑定 | 绑定对象 | 独立对象 |

**项目关联**: 本项目用 Redisson 分布式锁而不是 JVM 级别的锁。因为后端可能多实例部署，JVM 锁只能锁本机线程，分布式锁能跨实例。Redisson 底层用 Redis 的 Lua 脚本实现，原理类似 ReentrantLock 的 AQS，但存储在 Redis 而不是内存。

---

#### Q: JVM 内存模型（运行时数据区）？

**答**:
```
┌──────────────────────────────────────────────┐
│                    JVM                        │
│  ┌─────────────┐  ┌────────────────────────┐ │
│  │ 线程私有      │  │ 线程共享                │ │
│  │             │  │                        │ │
│  │ 程序计数器PC │  │ 堆 (Heap)              │ │
│  │ 虚拟机栈    │  │   - 新生代 (Eden+S0+S1) │ │
│  │ 本地方法栈  │  │   - 老年代              │ │
│  │             │  │                        │ │
│  └─────────────┘  │ 方法区/元空间(Metaspace)│ │
│                    │   - 类信息、常量池       │ │
│                    └────────────────────────┘ │
└──────────────────────────────────────────────┘
```

- **堆**: 存放对象实例，GC 主要区域
- **虚拟机栈**: 每个方法执行时创建栈帧（局部变量表、操作数栈、方法出口）
- **方法区/元空间**: 存放类信息、常量、静态变量。JDK 8 之前叫永久代（-XX:MaxPermSize），8 之后叫元空间（使用本地内存）

---

#### Q: 垃圾回收算法？CMS 和 G1 的区别？

**答**:
- **标记-清除**: 标记存活对象，清除未标记的。缺点：内存碎片
- **标记-整理**: 标记后把存活对象压缩到一端。没有碎片但移动对象成本高
- **复制算法**: 分两块，只用一块，满了就把存活对象复制到另一块。新生代用（Eden → S0/S1）

**CMS vs G1**:
- CMS: 并发标记清除，低停顿。缺点：内存碎片、CPU 敏感、浮动垃圾
- G1: 分 Region，优先回收垃圾最多的 Region（Garbage First）。可预测停顿时间。JDK 9 默认

**项目关联**: Spring Boot 应用一般用 G1 GC。可以在启动参数加 `-XX:+UseG1GC -XX:MaxGCPauseMillis=200`。

---

### 4.2 Spring 框架八股

#### Q: Spring Bean 的生命周期？

**答**:
```
实例化 (构造器) → 属性注入 (@Autowired/@Value)
→ Aware 回调 (BeanNameAware, ApplicationContextAware)
→ BeanPostProcessor.postProcessBeforeInitialization()
→ @PostConstruct → InitializingBean.afterPropertiesSet() → init-method
→ BeanPostProcessor.postProcessAfterInitialization()
→ Bean 就绪 → @PreDestroy → DisposableBean.destroy() → destroy-method → 销毁
```

**项目关联**: `RedissonConfig` 中 `@Bean(destroyMethod = "shutdown")` 就是用的 destroy-method，确保应用关闭时释放 Redisson 连接。

---

#### Q: Spring AOP 两种代理方式？

**答**:
- **JDK 动态代理**: 目标类实现了接口时使用。基于 `java.lang.reflect.Proxy`，代理接口
- **CGLIB 代理**: 目标类没有实现接口时使用。基于字节码生成子类，代理类本身

Spring Boot 2.x 默认使用 CGLIB（`spring.aop.proxy-target-class=true`）。

**项目关联**: `OperationLogAspect` 切面拦截 Controller 方法。Controller 通常不实现接口，所以用 CGLIB 代理。

---

#### Q: @Transactional 注解的传播行为？

**答**: 最常用的两种：
- **REQUIRED（默认）**: 当前有事务就加入，没有就新建
- **REQUIRES_NEW**: 挂起当前事务，新建一个独立事务

**项目关联**: `SyncServiceImpl.upload()` 使用 `@Transactional(rollbackFor = Exception.class)`，rollbackFor 确保 checked Exception 也回滚（默认只回滚 RuntimeException）。

---

#### Q: Spring Boot 自动配置原理？

**答**:
1. `@SpringBootApplication` 包含 `@EnableAutoConfiguration`
2. `@Import(AutoConfigurationImportSelector.class)` 扫描 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`（3.x 新机制，旧版是 spring.factories）
3. 找到自动配置类后，通过 `@ConditionalOnClass`、`@ConditionalOnMissingBean` 等条件注解按需加载

**项目关联**: 引入 `spring-boot-starter-data-redis` 后，`RedisAutoConfiguration` 自动创建 RedisTemplate。我在 `RedisConfig.java` 中自定义了 RedisTemplate 覆盖默认的序列化方式。

---

### 4.3 Redis 八股

#### Q: Redis 五种基本数据类型？

**答**:
- **String**: 最基本。项目中用于缓存用户信息（`user:info:{id}`）、限流计数（`rate:api:{ip}`）
- **List**: 双端队列。可用于消息队列（本项目未使用）
- **Set**: 无序去重集合。项目中用于在线用户追踪（`user:online`）
- **Hash**: 键值对集合。可用于存储对象的部分字段（本项目用 String 存整个对象）
- **Sorted Set**: 有序集合。可用于排行榜（RedisConstant 中预留了 `ranking:` 前缀但未实现）

扩展类型：HyperLogLog（基数统计）、Bitmap（位图，签到场景）、Geospatial（地理位置）。

---

#### Q: Redis 持久化方式？

**答**:
- **RDB（快照）**: 定时把内存数据 dump 到磁盘。优点：恢复快、文件紧凑。缺点：可能丢失最后一次快照后的数据
- **AOF（追加日志）**: 每条写命令追加到文件。优点：最多丢 1 秒数据（everysec 策略）。缺点：文件大、恢复慢
- 生产环境一般 RDB + AOF 混合使用

**项目关联**: 本项目是开发环境，Redis 持久化不是重点。但面试可能问，需要知道。

---

#### Q: Redis 内存淘汰策略？

**答**: 8 种策略，分两组：
- **不淘汰**: noeviction（内存满了直接报错）
- **淘汰所有 key**: allkeys-lru、allkeys-lfu、allkeys-random
- **淘汰设置了 TTL 的 key**: volatile-lru、volatile-lfu、volatile-random、volatile-ttl

**推荐**: 一般用 allkeys-lru（最近最少使用）。

---

#### Q: 缓存穿透、击穿、雪崩的区别和解决方案？

**答**:

| 问题 | 本质 | 解决方案 |
|------|------|----------|
| 穿透 | 查不存在的 key，缓存没有，每次都打 DB | 缓存空值（短 TTL）/ 布隆过滤器 |
| 击穿 | 热点 key 过期，大量并发请求同时打 DB | 互斥锁（分布式锁重建缓存）/ 热点 key 永不过期 |
| 雪崩 | 大量 key 同时过期 / Redis 宕机 | 随机 TTL / 多级缓存 / Redis 高可用（哨兵/集群） |

**项目关联**: 本项目所有 Redis 操作都有 try-catch 降级（穿透/雪崩时直接查 MySQL）。`RedisConstant` 中预留了 `bloom:user:id` 布隆过滤器 key（未实现），说明设计时考虑过穿透问题。

---

#### Q: Redisson 分布式锁的 WatchDog 机制？

**答**:
1. 加锁成功后，Redisson 启动后台定时任务（WatchDog）
2. 每隔 `lockWatchdogTimeout / 3`（默认 10 秒）检查锁是否还被当前线程持有
3. 如果还持有，把锁的过期时间重置为 30 秒
4. 业务执行完毕调用 unlock，WatchDog 停止

**为什么需要 WatchDog**: 如果业务执行时间超过锁的过期时间，锁会被 Redis 自动释放，其他线程就能拿到锁，导致并发问题。WatchDog 自动续期解决了这个问题。

**项目关联**: `SyncServiceImpl.upload()` 中 `tryLock(3, 10, TimeUnit.SECONDS)` 表示锁最多持 10 秒。如果上传逻辑超过 10 秒（不太可能），WatchDog 会续期。

---

### 4.4 MySQL 八股

#### Q: InnoDB 的索引结构？为什么用 B+ 树？

**答**:
- **B+ 树**: 非叶子节点只存 key，叶子节点存 key+data，叶子节点之间有双向链表
- **为什么不用 B 树**: B 树的非叶子节点也存 data，单个节点能存的 key 更少，树更高，IO 次数更多
- **为什么不用哈希**: 哈希只支持等值查询，不支持范围查询和排序
- **为什么不用二叉树**: 二叉树高度大（log2(n)），磁盘 IO 多。B+ 树是多叉的（一个节点存几百个 key），高度很低（3~4 层就能存千万级数据）

**聚簇索引 vs 非聚簇索引**:
- 聚簇索引（主键索引）：叶子节点存完整行数据
- 非聚簇索引（二级索引）：叶子节点存主键值。查询时先走二级索引找到主键，再回表查聚簇索引

---

#### Q: 事务的 ACID 和隔离级别？

**答**:
- **A（原子性）**: 事务要么全部成功，要么全部回滚（undo log 实现）
- **C（一致性）**: 事务前后数据满足约束
- **I（隔离性）**: 并发事务互不干扰
- **D（持久性）**: 事务提交后数据永久保存（redo log 实现）

隔离级别:
| 级别 | 脏读 | 不可重复读 | 幻读 |
|------|:---:|:---:|:---:|
| READ UNCOMMITTED | Y | Y | Y |
| READ COMMITTED | N | Y | Y |
| REPEATABLE READ (MySQL 默认) | N | N | Y |
| SERIALIZABLE | N | N | N |

**项目关联**: MySQL 默认 REPEATABLE READ。`@Transactional` 用的是数据库默认隔离级别。

---

#### Q: MyBatis 的 #{} 和 ${} 的区别？

**答**:
- `#{}`: 预编译参数占位符 `?`，防止 SQL 注入
- `${}`: 字符串拼接，直接替换到 SQL 中，有 SQL 注入风险

**项目关联**: 本项目主要用 MyBatis-Plus 的 LambdaQueryWrapper（类型安全），少数 XML SQL 中用 `#{}`（如 UserItemMapper.xml 第 22 行的 `#{username}`）。

---

### 4.5 网络 / 协议八股

#### Q: TCP 三次握手和四次挥手？

**答**:

**三次握手（建立连接）**:
1. 客户端 → SYN=1, seq=x → 服务端
2. 服务端 → SYN=1, ACK=1, seq=y, ack=x+1 → 客户端
3. 客户端 → ACK=1, seq=x+1, ack=y+1 → 服务端

**为什么是三次不是两次**: 防止已失效的连接请求到达服务端。如果是两次，服务端收到旧 SYN 就建立连接，浪费资源。

**四次挥手（断开连接）**:
1. 客户端 → FIN=1 → 服务端
2. 服务端 → ACK=1 → 客户端（半关闭状态，服务端还能发数据）
3. 服务端 → FIN=1 → 客户端
4. 客户端 → ACK=1 → 服务端

---

#### Q: Cookie、Session、Token 的区别？

**答**:

| 维度 | Cookie | Session | Token |
|------|--------|---------|-------|
| 存储位置 | 客户端浏览器 | 服务端内存/Redis | 客户端（localStorage/Header） |
| 安全性 | 低（可被篡改） | 中（服务端控制） | 高（签名验证） |
| 扩展性 | 差（依赖 Cookie） | 差（需要 Session 共享） | 好（无状态） |
| 跨域 | 受限 | 受限 | 支持 |

**项目关联**: 本项目用 JWT Token，无状态。Token 存在客户端 localStorage，每次请求通过 `Authorization: Bearer xxx` 请求头发送。

---

#### Q: HTTP 状态码？

**答**:
- **2xx**: 200 OK, 201 Created, 204 No Content
- **3xx**: 301 永久重定向, 302 临时重定向, 304 Not Modified（协商缓存）
- **4xx**: 400 Bad Request, 401 未认证, 403 无权限, 404 Not Found, 429 请求过多
- **5xx**: 500 服务器错误, 502 Bad Gateway, 503 服务不可用

**项目关联**: 限流时返回 HTTP 429，认证失败返回 401/403。

---

### 4.6 场景题

#### Q: 你们的 JWT Token 存在安全风险吗？怎么改进？

**答**:
当前风险点：
1. **Token 无法主动失效**: JWT 是无状态的，签发后在过期前一直有效。如果 Token 泄露，无法撤销
2. **Token 存在 localStorage**: 可能被 XSS 攻击窃取
3. **没有 Refresh Token**: 过期后需要重新登录

改进方案：
1. **引入 Redis 黑名单**: Token 登出时加入 Redis 黑名单（key=token, TTL=剩余有效期），每次请求检查是否在黑名单中
2. **存入 HttpOnly Cookie**: 防止 JS 读取，防 XSS
3. **实现 Refresh Token**: Access Token 短期（15 分钟），Refresh Token 长期（7 天），存数据库可撤销

---

#### Q: 如果让你实现滑动窗口限流，怎么做？

**答**:

方案 1: Redis Sorted Set
```
Key: rate:api:{ip}
每次请求: ZADD key timestamp timestamp
过期清理: ZREMRANGEBYSCORE key 0 (now - window)
计数: ZCARD key
判断: count > maxRequests ? 拒绝 : 放行
```
每个请求存一条记录，精确但内存开销大。

方案 2: 两次固定窗口加权
```
当前窗口计数 = count_current
上一个窗口计数 = count_prev
加权请求数 = count_prev * (已过时间/窗口) + count_current
```
精度和内存的折中。

---

#### Q: 分布式锁的几种实现方式？各有什么优缺点？

**答**:

| 方式 | 优点 | 缺点 |
|------|------|------|
| MySQL 唯一索引 | 简单，无额外依赖 | 性能差，无法自动过期 |
| Redis SETNX | 性能好，支持过期 | 不支持可重入，主从切换可能丢锁 |
| Redisson | 可重入、WatchDog 续期、Lua 原子 | 依赖 Redis，主从切换仍可能丢锁 |
| ZooKeeper | 强一致，临时节点自动删除 | 性能比 Redis 差，部署复杂 |
| etcd | 强一致，Raft 协议 | 部署复杂 |

**Redis 主从切换丢锁问题**: 客户端 A 在 Master 加锁成功，Master 还没同步到 Slave 就宕机，Slave 提升为新 Master，客户端 B 也能加锁成功。

**Redlock 算法**: 多个独立 Redis 节点（>=5），加锁需要在多数节点（>=3）成功才算成功。解决了主从切换问题但有争议（Martin Kleppmann 和 Antirez 的辩论）。

---

#### Q: 你的项目如果要支持百万用户，数据库怎么优化？

**答**:
1. **读写分离**: 主库写、从库读。MySQL 主从复制 + 中间件（ShardingSphere / MyCat）
2. **分库分表**: 用户表按 userId 哈希分片（如 16 张表）。存档表按 userId 分片
3. **索引优化**: EXPLAIN 分析慢查询，确保 WHERE 条件列有索引
4. **连接池调优**: HikariCP 最大连接数 = CPU 核数 * 2 + 磁盘数
5. **批量操作**: 减少频繁的单条 INSERT/UPDATE，用批量操作
6. **冷热分离**: 活跃用户数据在 MySQL，历史数据归档到历史表或 ES

---

## 五、高并发场景题专项

这部分是面试中经常出现的「假设你的系统有百万/千万用户」场景题。

### 5.1 如何设计一个秒杀系统？

**核心难点**: 瞬时高并发、库存超卖、系统保护

**方案**:
```
客户端 → Nginx（限流+静态化）→ 网关（令牌桶限流）
→ Redis（库存预扣减 DECR + Lua 脚本保证原子性）
→ MQ（削峰填谷）→ 数据库（异步下单扣减）
```

**关键点**:
1. **库存预热**: 活动开始前把库存加载到 Redis
2. **Lua 脚本原子扣减**: `if redis.call('GET', key) > 0 then redis.call('DECR', key) return 1 else return 0 end`
3. **一人一单**: 用 Redis Set 记录已购买用户 `SADD seckill:users:goodsId userId`，SISMEMBER 检查
4. **MQ 异步下单**: Redis 扣减成功后发 MQ 消息，消费者异步创建订单
5. **限流降级**: Nginx 层限流 → 网关层限流 → 应用层限流（本项目的 @RateLimit 就是应用层）

**项目关联**: 我的项目中 `RedisConstant` 预留了 `lock:shop:item:` 和 `lock:user:purchase:` 锁 key，说明设计时考虑过商城抢购场景。

---

### 5.2 如何解决缓存和数据库双写一致性问题？

**三种策略**:

**1. Cache-Aside（本项目使用）**
```
读: 查缓存 → 命中返回 / 未命中查 DB → 回写缓存
写: 更新 DB → 删除缓存
```
为什么先更新 DB 再删缓存？如果先删缓存再更新 DB，删完缓存后另一个请求查到旧数据并回写缓存，永远不一致。

**2. 延迟双删**
```
删缓存 → 更新 DB → 延迟 500ms → 再删缓存
```
解决并发读写时的短暂不一致：更新 DB 后，另一个线程可能已经把旧数据读入缓存了，延迟再删一次。

**3. 订阅 Binlog（最终一致性）**
```
更新 DB → Canal 监听 Binlog → 发 MQ → 消费者删除/更新缓存
```
解耦业务代码和缓存操作，最终一致性保证。

---

### 5.3 如何设计一个排行榜系统？

**方案**: Redis Sorted Set
```
ZADD ranking:daily {score} {userId}    -- 添加/更新分数
ZREVRANGE ranking:daily 0 9 WITHSCORES -- 查询前 10 名
ZREVRANK ranking:daily {userId}        -- 查询某人排名
```

**为什么用 Sorted Set**: O(logN) 的插入和排名查询，天然适合排行榜。

**项目关联**: `RedisConstant.RANKING = "ranking:"` 已经预留了排行榜 key 前缀。

---

### 5.4 如何实现分布式 Session？

**方案**: Spring Session + Redis
```
1. 引入 spring-session-data-redis
2. Session 自动存储到 Redis
3. 多个应用实例共享同一个 Session
```

但本项目用 JWT 不需要 Session。JWT 本身就是无状态的，不需要服务端存储会话信息。

---

### 5.5 消息队列的使用场景？如何保证消息不丢失？

**使用场景**:
1. 异步处理（发邮件/短信/推送通知）
2. 削峰填谷（秒杀、抢购）
3. 解耦（订单系统和库存系统）

**保证不丢失**:
1. **生产者确认**: RabbitMQ 用 Confirm 模式，Kafka 用 acks=all
2. **Broker 持久化**: RabbitMQ 持久化队列+消息，Kafka 多副本
3. **消费者确认**: 手动 ACK，处理完才确认

**本项目关联**: 如果未来需要优化存档上传，可以用 MQ 异步写入数据库，上传接口立即返回成功，后台异步处理。

---

### 5.6 如何设计一个延迟任务系统？

**方案对比**:

| 方案 | 优点 | 缺点 |
|------|------|------|
| 定时轮询 DB | 简单 | 精度差、DB 压力大 |
| Redis 过期监听 | 简单 | 不可靠（通知可能丢失） |
| Redis Sorted Set | 可靠、精度高 | 需要自己轮询 |
| RabbitMQ 延迟队列 | 可靠 | 部署复杂 |
| RocketMQ 延迟消息 | 原生支持 | 精度有限（固定级别） |

**Redis Sorted Set 方案**: `ZADD delay:task {executeTime} {taskData}`，定时任务 `ZRANGEBYSCORE delay:task 0 {now}` 取到期任务执行。

**项目关联**: `OnlineUserCleanupTask` 用 `@Scheduled(fixedRate = 300000)` 做定时清理，这就是最简单的定时任务方案。如果清理逻辑复杂或需要精确到秒级，可以考虑 Sorted Set。

---

### 5.7 线程池的核心参数？如何合理配置？

**答**:
```java
new ThreadPoolExecutor(
    corePoolSize,     // 核心线程数
    maximumPoolSize,  // 最大线程数
    keepAliveTime,    // 非核心线程空闲存活时间
    TimeUnit,         // 时间单位
    workQueue,        // 任务队列
    threadFactory,    // 线程工厂
    handler           // 拒绝策略
);
```

**拒绝策略 4 种**:
1. AbortPolicy: 抛 RejectedExecutionException（默认）
2. CallerRunsPolicy: 调用者线程执行（降级）
3. DiscardPolicy: 静默丢弃
4. DiscardOldestPolicy: 丢弃队列最老的任务

**配置公式**:
- CPU 密集型: 核心线程数 = CPU 核数 + 1
- IO 密集型: 核心线程数 = CPU 核数 * 2（或 CPU 核数 / (1 - 阻塞系数)）

**项目关联**: Spring Boot 默认使用 Tomcat 线程池处理请求（默认 max-threads=200）。`@Scheduled` 使用单线程的 TaskScheduler，如果需要并行执行多个定时任务需要配置线程池。

---

### 5.8 如何排查线上 OOM 问题？

**步骤**:
1. **确认现象**: 哪个服务报错？错误信息是什么？（OutOfMemoryError: Java heap space / Metaspace / ...）
2. **获取 dump**: `jmap -dump:live,format=b,file=heap.hprof {pid}`
3. **分析工具**: MAT (Memory Analyzer Tool) / VisualVM / JProfiler
4. **找大对象**: 查看 Dominator Tree，找到占用内存最多的对象
5. **分析 GC**: `jstat -gcutil {pid} 1000` 看 GC 频率和回收效果
6. **修复**: 如果是内存泄漏（如 HashMap 只 put 不 remove），修复代码。如果确实需要更多内存，调整 -Xmx

---

### 5.9 如何设计一个短链接系统？

**核心流程**:
```
长链接 → 计算 hash/MurmurHash → Base62 编码 → 短码
短码 → 查 Redis → 命中返回 / 未命中查 DB → 302 重定向
```

**关键点**:
1. **短码生成**: MurmurHash(长链接) → Base62 编码 → 6 位短码（62^6 ≈ 568 亿种组合）
2. **哈希冲突**: 冲突时追加随机字符重新哈希，或使用自增 ID + Base62
3. **缓存**: 热门短链接缓存在 Redis，TTL 根据访问频率动态调整
4. **302 而不是 301**: 302 临时重定向，每次都会经过服务端（可以统计点击量）。301 永久重定向，浏览器会缓存

---

### 5.10 如果让你重新设计项目，怎么做高可用？

**答**:
```
                    ┌──── Nginx (限流+负载均衡+静态资源)
                    │
               ┌────┴────┐
               │  Gateway │ (统一认证、全局限流、路由)
               └────┬────┘
          ┌─────────┼─────────┐
     ┌────┴───┐ ┌───┴───┐ ┌──┴────┐
     │ App-1  │ │ App-2 │ │ App-3 │ (无状态水平扩展)
     └────┬───┘ └───┬───┘ └──┬────┘
          │         │        │
     ┌────┴─────────┴────────┴────┐
     │       Redis Cluster         │ (缓存+分布式锁+限流)
     └────┬───────────────┬───────┘
     ┌────┴───┐      ┌────┴───┐
     │Master  │──主从─│ Slave  │  (MySQL 读写分离)
     └────────┘      └────────┘
```

**关键改进**:
1. **Nginx 负载均衡**: 多实例部署，Nginx 做反向代理（轮询/加权/IP Hash）
2. **网关层**: Spring Cloud Gateway 统一认证、全局限流、路由
3. **Redis Cluster**: 替换单机 Redis，支持水平扩展和故障转移
4. **MySQL 主从**: 读写分离，从库分担查询压力
5. **MQ**: 存档上传等非实时操作走 MQ 异步处理
6. **监控**: Prometheus + Grafana 监控 QPS、延迟、错误率

---

## 六、多余文件清单（可删除）

以下是项目中可以安全删除的冗余/无用文件：

### 1. 完全无用的文件

| 文件 | 原因 |
|------|------|
| `backend/src/main/resources/mapper/UserItemMapper.xml` | 引用了不存在的 `t_user_item` 表，没有任何 Java 代码调用此 Mapper |
| `backend/src/main/java/com/pixelsurvivor/entity/vo/UserItemVO.java` | 配合上面的 XML 使用，但 `t_user_item` 表不存在，完全无用 |
| `docs/resume.md` | 内容与根目录 `面试准备与简历指南.md` 重复 |

### 2. RedisConstant 中预留但未使用的常量

这些常量在代码中没有被引用（除定义处外），可以暂时保留作为设计意图的记录，但如果想精简可以删除：

| 常量 | 说明 |
|------|------|
| `SHOP_ITEMS_LIST` | 商品列表缓存——商城功能未实现 |
| `SHOP_ITEM_DETAIL` | 商品详情缓存——商城功能未实现 |
| `SHOP_ITEMS_TYPE` | 按类型商品列表——商城功能未实现 |
| `RANKING` | 排行榜——功能未实现 |
| `FRIEND_LIST` | 好友列表——功能未实现 |
| `SIGN_RECORD` | 签到记录——功能未实现 |
| `LOCK_SHOP_ITEM` | 商品锁——商城功能未实现 |
| `LOCK_USER_PURCHASE` | 用户购买锁——商城功能未实现 |
| `LOCK_SIGN` | 签到锁——功能未实现 |
| `BLOOM_USER_ID` | 用户布隆过滤器——未实现 |
| `BLOOM_SHOP_ITEM_ID` | 商品布隆过滤器——未实现 |
| `RATE_PURCHASE` | 购买限流——商城功能未实现 |
| `CACHE_LOGICAL_EXPIRE` | 缓存逻辑过期——未实现 |

### 3. ResultCode 中预留但未使用的枚举

| 枚举值 | 说明 |
|--------|------|
| `COIN_NOT_ENOUGH` ~ `BUY_LIMIT_EXCEEDED` (2001-2005) | 商城相关，功能未实现 |
| `FRIEND_REQUEST_SENT` / `ALREADY_FRIENDS` (3001-3002) | 好友相关，功能未实现 |
| `ALREADY_SIGNED` ~ `TASK_REWARD_ALREADY_CLAIMED` (4001-4003) | 签到/任务，功能未实现 |
| `ITEM_NOT_ENOUGH` (5001) | 背包，功能未实现 |
| `CHARACTER_ALREADY_UNLOCKED` / `CHARACTER_NOT_UNLOCKED` (6001-6002) | 角色，功能未实现 |
| `ACHIEVEMENT_NOT_COMPLETED` / `ACHIEVEMENT_REWARD_ALREADY_CLAIMED` (7001-7002) | 成就，功能未实现 |

### 4. 其他冗余

| 文件 | 说明 |
|------|------|
| `.idea/` 目录 | IDE 配置文件，应加入 `.gitignore` |
| `.vscode/` 目录 | IDE 配置文件，应加入 `.gitignore` |
| `pixel-survivor-game/backend/src/test/java/.../PasswordEncoderTest.java` | 只是一个临时测试，但保留也无妨 |

### 建议的清理动作

```bash
# 1. 删除确定无用的文件
rm backend/src/main/resources/mapper/UserItemMapper.xml
rm backend/src/main/java/com/pixelsurvivor/entity/vo/UserItemVO.java

# 2. 将 IDE 配置加入 .gitignore（如果还没有的话）
echo ".idea/" >> .gitignore
echo ".vscode/" >> .gitignore
```

---

## 附录: 面试前 30 分钟快速自检

1. [ ] 能用 30 秒讲清楚项目是什么
2. [ ] 能画出系统架构图（3 层 + Redis + MySQL）
3. [ ] 能说出 22 个 API 的大致分类
4. [ ] 能说出 9 张表的名字和用途
5. [ ] 能说出 Redis 5 种场景及降级策略
6. [ ] 能说出 3 个拦截器的顺序和作用
7. [ ] 能说出 JWT 的三部分结构和签名验证原理
8. [ ] 能说出 AOP 操作日志的完整流程
9. [ ] 准备好 2~3 个 STAR 法则场景
10. [ ] 能回答「如果重新做会怎么改进」
11. [ ] 能说出 ConcurrentHashMap 原理
12. [ ] 能说出 B+ 树索引结构
13. [ ] 能说出缓存穿透/击穿/雪崩的区别和方案
14. [ ] 能说出秒杀系统的设计思路
